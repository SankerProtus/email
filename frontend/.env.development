# Development Environment Variables
# Leave empty to use Vite proxy (configured in vite.config.js)
# The proxy forwards /api requests to http://localhost:5000
# You can also set it directly: VITE_API_URL=http://localhost:5000
VITE_API_URL=
